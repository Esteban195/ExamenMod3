pipeline {
    agent any{
    stage('Newman Tests') {
        steps {
            bat '''
            cd api/newman
            newman run ExamenModulo3.json -e ExamenMod3.json
            '''
        }
    }
    stage('JMeter Tests') {
        steps {
            bat '''
            cd load
            jmeter -n -t ExamenU.jmx
            '''
        }
    }
    }
}